#include <stdio.h>

#include <stdlib.h>

#define MAX 1000;

typedef struct post{
    int id,g,like;                      //dichirazione variabili//
    
    char mese[30];                      //dichiarazione variabile char//

}instP;

int main(){
    
    
       instP *vetp;
       FILE *s;
        FILE *p;

    int c  = 0;                     //assegnamento valore a variabile//
    
    
     char vet[MAX];                     

    
    
    p=fopen("instagram.csv","r");        //apertura del file csv//

    if(p){
        i
        for (c = getc(s); c != EOF; c = getc(s)){
            if (c == '\n')
                  
                  
                   count++;
        }
    
    }

            fclose(p);



    vetp = (instP *)malloc(sizeof instP)*c);
    s = fopen("instagram.csv","r");

    
    
    
    
    
    if (s){
        vetp = (instP *) mallpc(sizeof)
    }


}